# GitHub 直接预览单文件版

这是最省心的 GitHub Pages 上传版本。

上传方法：

1. 打开本文件夹。
2. 只需要把 `index.html` 和 `.nojekyll` 上传到 GitHub 仓库根目录。
3. 手机访问 GitHub Pages 地址即可预览。

注意：

- 不要上传外层文件夹本身，要上传文件夹里面的文件。
- 这个版本不是给前端继续开发用的，只用于预览展示。
