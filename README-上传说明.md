# 商户资金看板 GitHub 上传说明

这个文件夹是“商户资金看板”第一版静态预览包。

上传到 GitHub 时：

1. 把 `index.html` 上传到仓库根目录。
2. 在 GitHub Pages 里选择从根目录发布。
3. 手机打开 GitHub Pages 地址即可预览。

说明：

- 这是单文件版本，图片、样式和脚本都已经打进 `index.html` 里。
- 不需要上传 `node_modules`。
- 不需要上传 `src`、`dist`、`package.json` 等开发文件。
- 如果后续要接接口，再把完整开发项目交给前端工程师即可。
